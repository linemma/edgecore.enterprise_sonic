plugin_routing:
  action:
    sonic_config:
      redirect: edgecore.sonic.sonic
    sonic_command:
      redirect: edgecore.sonic.sonic
    sonic_api:
      redirect: edgecore.sonic.sonic
    sonic_lag_interfaces:
      redirect: edgecore.sonic.sonic
    sonic_interfaces:
      redirect: edgecore.sonic.sonic
    sonic_l2_interfaces:
      redirect: edgecore.sonic.sonic
    sonic_l3_interfaces:
      redirect: edgecore.sonic.sonic
    sonic_vlans:
      redirect: edgecore.sonic.sonic
    sonic_facts:
      redirect: edgecore.sonic.sonic
